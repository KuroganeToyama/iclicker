EMAIL = 'insert your email address'
PASSWORD = 'insert your password'
DOWNLOAD_PATH = 'insert the full path to your Downloads folder (or whichever folder that stores the downloaded files when you manually download them from iClicker Cloud, since iClicker Cloud does not let you choose the download path and only uses the browser default)'
EXCEPTIONS = ['a list of polls to ignore. for example: EXCEPTIONS = ["Class 1 - Poll", "Class 2 - Poll"]']